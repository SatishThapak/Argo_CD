apiVersion: v1
kind: Service
metadata:
  name: apache-service
  namespace: apache-demo
spec:
  selector:
    app: apache-server
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
